// sum of first n natural number

#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;
    cout << "Sum of first n natural number is : " << (n * (n + 1) / 2);
    return 0;
}