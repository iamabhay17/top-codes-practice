// check weather a character is alphabet or not

#include <iostream>
using namespace std;

int main()
{
    char c;
    cin >> c;

    isalpha(c) ? cout << "Alphabet" : cout << "Not alphabet";

    return 0;
}